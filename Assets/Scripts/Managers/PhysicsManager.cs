using UnityEngine;

/// <summary>
/// In this project we needed to make the gravity adequate to the scale of the objects, since the objects are so big we needed to set a very higher gravity to make the spheres fall at a speed similar to that which would fall on Earth.
/// </summary>
public class PhysicsManager : MonoBehaviour
{
    public float gravity;
}
